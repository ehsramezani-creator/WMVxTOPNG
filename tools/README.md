# MPQ tooling

The project does not depend on a local copy of another working repository.

For local WoW 3.3.5a testing, build `MPQExtractor` from the upstream Git repository:
https://github.com/Kanma/MPQExtractor

The Node adapter in `src/mpq/MPQExtractor.js` accepts the resulting executable path.
